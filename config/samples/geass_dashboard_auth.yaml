apiVersion: v1
kind: Secret
metadata:
  name: geass-dashboard-auth
  namespace: geass-system
type: Opaque
stringData:
  # Replace CHANGE_ME before applying. Geass refuses that placeholder
  # password and hashes a real password on first login.
  users: |
    [{"username":"admin","password":"CHANGE_ME","role":"admin"}]
