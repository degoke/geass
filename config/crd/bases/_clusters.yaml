---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: clusters.
spec:
  group: ""
  names:
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    singular: cluster
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: Cluster is a CloudNativePG PostgreSQL cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterSpec defines a CNPG cluster.
            properties:
              bootstrap:
                description: BootstrapConfiguration defines initial database bootstrap.
                properties:
                  initdb:
                    description: InitDBConfiguration defines initdb settings.
                    properties:
                      database:
                        type: string
                      owner:
                        type: string
                      secret:
                        description: |-
                          LocalObjectReference contains enough information to let you locate the
                          referenced object inside the same namespace.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                type: object
              imageName:
                type: string
              instances:
                format: int32
                type: integer
              storage:
                description: StorageConfiguration defines PVC settings.
                properties:
                  size:
                    type: string
                type: object
            type: object
          status:
            description: ClusterStatus defines observed CNPG cluster state.
            properties:
              currentPrimary:
                type: string
              phase:
                type: string
              readyInstances:
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
