---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: geassdatabases.geass.geass.dev
spec:
  group: geass.geass.dev
  names:
    kind: GeassDatabase
    listKind: GeassDatabaseList
    plural: geassdatabases
    singular: geassdatabase
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.environment
      name: Environment
      type: string
    - jsonPath: .spec.engine
      name: Engine
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GeassDatabase is the Schema for the geassdatabases API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GeassDatabaseSpec defines the desired state of GeassDatabase.
            properties:
              connectionRef:
                description: ConnectionRef identifies a GeassCloudConnection used
                  for PlanetScale or AWS.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              databaseName:
                description: DatabaseName overrides the default database name for
                  external or logical-style servers.
                type: string
              engine:
                description: Engine is the database engine to provision.
                enum:
                - Postgres
                - MySQL
                - SQLite
                - Redis
                type: string
              environment:
                description: Environment is the project environment.
                maxLength: 63
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              externalHost:
                description: ExternalHost is the hostname used when connecting to
                  an existing server.
                type: string
              externalPort:
                description: ExternalPort is the port used when connecting to an existing
                  server.
                format: int32
                type: integer
              highAvailability:
                description: |-
                  HighAvailability requests a multi-instance topology. It is only valid when the
                  cluster HA readiness check reports at least three healthy nodes.
                type: boolean
              instances:
                default: 1
                description: Instances is the number of in-cluster instances. HighAvailability
                  forces at least 3.
                format: int32
                minimum: 1
                type: integer
              mode:
                description: Mode selects whether Geass should create a remote database
                  or connect to an existing one.
                enum:
                - Create
                - Connect
                type: string
              passwordSecretRef:
                description: PasswordSecretRef points at the Secret that stores the
                  external password.
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              placement:
                default: InCluster
                description: Placement selects in-cluster provisioning or an external
                  provider.
                enum:
                - InCluster
                - External
                type: string
              project:
                description: Project is the owning project.
                minLength: 1
                type: string
              provider:
                description: Provider is required when Placement is External.
                enum:
                - PlanetScale
                - AWS
                type: string
              storageSize:
                anyOf:
                - type: integer
                - type: string
                default: 10Gi
                description: StorageSize is the requested PVC size per instance.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              username:
                description: Username is the login used when connecting to an existing
                  server.
                type: string
              version:
                default: "16"
                description: Version is the engine major version for in-cluster servers.
                type: string
            required:
            - engine
            - environment
            - project
            type: object
          status:
            description: GeassDatabaseStatus defines the observed state of GeassDatabase.
            properties:
              conditions:
                description: conditions represent the current state of the GeassDatabase
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              connectionSecret:
                description: ConnectionSecret is the name of the Secret containing
                  connection credentials.
                type: string
              host:
                description: Host is the in-cluster database hostname.
                type: string
              targetNamespace:
                description: TargetNamespace is the namespace workloads were created
                  in.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
