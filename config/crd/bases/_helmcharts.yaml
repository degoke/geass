---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: helmcharts.
spec:
  group: ""
  names:
    kind: HelmChart
    listKind: HelmChartList
    plural: helmcharts
    singular: helmchart
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: HelmChart is the K3s Helm controller chart resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HelmChartSpec defines the desired Helm chart installation.
            properties:
              bootstrap:
                type: boolean
              chart:
                type: string
              chartContent:
                type: string
              createNamespace:
                type: boolean
              failurePolicy:
                type: string
              helmVersion:
                type: string
              jobImage:
                type: string
              repo:
                type: string
              set:
                additionalProperties:
                  type: string
                type: object
              targetNamespace:
                type: string
              timeout:
                type: string
              valuesContent:
                type: string
              version:
                type: string
            type: object
          status:
            description: HelmChartStatus defines the observed state of a HelmChart.
            properties:
              conditions:
                items:
                  description: HelmChartCondition describes HelmChart readiness.
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
              failureMessage:
                type: string
              jobName:
                type: string
              url:
                type: string
              version:
                type: integer
            type: object
        type: object
    served: true
    storage: true
