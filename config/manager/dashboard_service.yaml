apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: geass
    app.kubernetes.io/component: dashboard
  name: geass-dashboard
  namespace: system
spec:
  type: ClusterIP
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: geass
  ports:
  - name: http
    port: 8082
    targetPort: dashboard
    protocol: TCP
